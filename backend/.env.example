# MongoDB Configuration
MONGODB_URI=mongodb+srv://Teamera:<db_password>@teamera.aqye1n4.mongodb.net/?appName=Teamera

# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Configuration
JWT_SECRET=your-jwt-secret-key-here-generate-with-crypto
JWT_EXPIRE=7d

# CORS Configuration
FRONTEND_URL=http://localhost:5173

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Instructions:
# 1. Copy this file to .env
# 2. Replace <db_password> with your actual MongoDB password
# 3. Generate JWT_SECRET using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 4. Never commit the .env file to version control
